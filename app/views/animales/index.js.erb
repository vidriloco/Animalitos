$('#index-animales').html("<%= escape_javascript render('animales') %>");
$('.pagination a').attr('data-remote', 'true');

var baseurl = location.protocol + '//' + location.host + location.pathname;
var params;

paginacionAjaxHistory('.pagination a');
